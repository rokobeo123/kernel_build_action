name: 🔋 Xiaomi 14 Pro - AngelBeats Style + SukiSU + SUSFS

on:
  workflow_dispatch:
    inputs:
      profile:
        description: 'Battery Profile'
        required: true
        default: 'balanced'
        type: choice
        options:
          - extreme_battery
          - balanced
          - performance
  
  push:
    branches:
      - main
    paths:
      - '.github/workflows/xiaomi14pro-angelbeats.yml'

jobs:
  build:
    name: Build Kernel
    runs-on: ubuntu-22.04
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
      
      - name: Set default profile
        run: |
          if [ -z "${{ inputs.profile }}" ]; then
            echo "PROFILE=balanced" >> $GITHUB_ENV
          else
            echo "PROFILE=${{ inputs.profile }}" >> $GITHUB_ENV
          fi
      
      - name: 🧹 Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache
          sudo apt-get clean
          df -h
      
      - name: 📦 Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential ccache curl flex \
            g++-multilib gcc-multilib git gnupg gperf imagemagick \
            lib32readline-dev lib32z1-dev liblz4-tool libncurses5-dev \
            libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop \
            pngcrush rsync schedtool squashfs-tools xsltproc zip \
            zlib1g-dev python3 python-is-python3 wget cpio kmod
      
      - name: 📂 Clone kernel source
        run: |
          echo "📥 Cloning kernel source..."
          git clone --depth=1 \
            https://github.com/MiCode/Xiaomi_Kernel_OpenSource \
            -b shennong-u-oss \
            kernel
          
          cd kernel
          echo "✅ Kernel cloned!"
          make kernelversion 2>/dev/null || cat Makefile | grep "^VERSION\|^PATCHLEVEL\|^SUBLEVEL" | head -3
      
      - name: 🔧 Fix missing Kconfig files
        working-directory: kernel
        run: |
          echo "🔧 Fixing missing Kconfig references..."
          
          # Fix drivers/misc/Kconfig - remove missing hwid reference
          if [ -f "drivers/misc/Kconfig" ]; then
            echo "Patching drivers/misc/Kconfig..."
            sed -i '/source "drivers\/misc\/hwid\/Kconfig"/d' drivers/misc/Kconfig
          fi
          
          # Create empty Kconfig if directory exists but file missing
          if [ -d "drivers/misc/hwid" ] && [ ! -f "drivers/misc/hwid/Kconfig" ]; then
            echo "Creating empty hwid Kconfig..."
            touch drivers/misc/hwid/Kconfig
          fi
          
          # Find and fix other potential missing Kconfig files
          grep -r "source.*Kconfig" drivers/ 2>/dev/null | grep -v ".git" | cut -d'"' -f2 | while read kconfig; do
            if [ ! -f "$kconfig" ]; then
              echo "Missing: $kconfig"
              # Comment out the source line
              find drivers/ -name "Kconfig" -exec sed -i "\|source.*$(basename $kconfig)|d" {} \; 2>/dev/null
            fi
          done
          
          echo "✅ Kconfig files fixed!"
      
      - name: 🔧 Integrate SukiSU
        working-directory: kernel
        run: |
          echo "🚀 Installing SukiSU..."
          curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s builtin
          
          if [ -d "KernelSU" ]; then
            echo "✅ SukiSU integrated!"
            ls -la KernelSU/kernel/ | head -10
          else
            echo "❌ SukiSU integration failed!"
            exit 1
          fi
      
      - name: 📥 Clone SUSFS and copy files
        working-directory: kernel
        run: |
          echo "📥 Cloning SUSFS..."
          git clone --depth=1 https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android14-6.1
          
          echo "Copying SUSFS source files..."
          
          # Method 1: From ksu_susfs
          if [ -d "susfs4ksu/ksu_susfs" ]; then
            cp -v susfs4ksu/ksu_susfs/*.c KernelSU/kernel/ 2>/dev/null || true
            cp -v susfs4ksu/ksu_susfs/*.h KernelSU/kernel/ 2>/dev/null || true
          fi
          
          # Method 2: From kernel_patches/ksu_susfs
          if [ -d "susfs4ksu/kernel_patches/ksu_susfs" ]; then
            cp -v susfs4ksu/kernel_patches/ksu_susfs/*.c KernelSU/kernel/ 2>/dev/null || true
            cp -v susfs4ksu/kernel_patches/ksu_susfs/*.h KernelSU/kernel/ 2>/dev/null || true
          fi
          
          # Method 3: Find all susfs files
          find susfs4ksu -type f \( -name "*susfs*.c" -o -name "*susfs*.h" \) -exec cp -v {} KernelSU/kernel/ \; 2>/dev/null || true
          
          echo "✅ SUSFS files copied!"
          echo "Files in KernelSU/kernel:"
          ls -la KernelSU/kernel/ | grep susfs || echo "No explicit susfs files (may be integrated)"
      
      - name: ⚡ Create battery optimization configs
        working-directory: kernel
        run: |
          echo "⚡ Creating battery configs..."
          mkdir -p arch/arm64/configs/vendor
          
          # Extreme Battery
          cat > arch/arm64/configs/vendor/angelbeats_extreme_battery.config << 'ENDCONFIG'
          CONFIG_KSU=y
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_SUS_OVERLAYFS=y
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KPM=y
          CONFIG_KPROBES=y
          CONFIG_HAVE_KPROBES=y
          CONFIG_KPROBE_EVENTS=y
          CONFIG_KALLSYMS=y
          CONFIG_KALLSYMS_ALL=y
          CONFIG_CPU_FREQ_GOV_POWERSAVE=y
          CONFIG_CPU_FREQ_GOV_SCHEDUTIL=y
          CONFIG_CPU_FREQ_DEFAULT_GOV_SCHEDUTIL=y
          CONFIG_CPU_IDLE=y
          CONFIG_IOSCHED_BFQ=y
          CONFIG_BFQ_GROUP_IOSCHED=y
          CONFIG_PM=y
          CONFIG_PM_WAKELOCKS_LIMIT=100
          CONFIG_ZRAM=y
          CONFIG_LZ4_COMPRESS=y
          CONFIG_THERMAL=y
          CONFIG_WIREGUARD=y
          ENDCONFIG
          
          # Balanced
          cat > arch/arm64/configs/vendor/angelbeats_balanced.config << 'ENDCONFIG'
          CONFIG_KSU=y
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_SUS_OVERLAYFS=y
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KPM=y
          CONFIG_KPROBES=y
          CONFIG_HAVE_KPROBES=y
          CONFIG_KPROBE_EVENTS=y
          CONFIG_KALLSYMS=y
          CONFIG_KALLSYMS_ALL=y
          CONFIG_CPU_FREQ_GOV_SCHEDUTIL=y
          CONFIG_CPU_FREQ_DEFAULT_GOV_SCHEDUTIL=y
          CONFIG_CPU_IDLE=y
          CONFIG_IOSCHED_BFQ=y
          CONFIG_BFQ_GROUP_IOSCHED=y
          CONFIG_PM=y
          CONFIG_PM_WAKELOCKS_LIMIT=200
          CONFIG_ZRAM=y
          CONFIG_LZ4_COMPRESS=y
          CONFIG_THERMAL=y
          CONFIG_WIREGUARD=y
          ENDCONFIG
          
          # Performance
          cat > arch/arm64/configs/vendor/angelbeats_performance.config << 'ENDCONFIG'
          CONFIG_KSU=y
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_SUS_OVERLAYFS=y
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
          CONFIG_KPM=y
          CONFIG_KPROBES=y
          CONFIG_HAVE_KPROBES=y
          CONFIG_KPROBE_EVENTS=y
          CONFIG_KALLSYMS=y
          CONFIG_KALLSYMS_ALL=y
          CONFIG_CPU_FREQ_GOV_SCHEDUTIL=y
          CONFIG_IOSCHED_BFQ=y
          CONFIG_BFQ_GROUP_IOSCHED=y
          CONFIG_PM_WAKELOCKS_LIMIT=500
          CONFIG_ZRAM=y
          CONFIG_LZ4_COMPRESS=y
          CONFIG_WIREGUARD=y
          ENDCONFIG
          
          echo "✅ Configs created!"
          ls -la arch/arm64/configs/vendor/angelbeats_*.config
      
      - name: 🔨 Clone Proton Clang
        run: |
          echo "📥 Cloning Proton Clang..."
          git clone --depth=1 https://github.com/kdrag0n/proton-clang clang
          echo "✅ Clang ready!"
      
      - name: ⚙️ Merge configs and prepare build
        working-directory: kernel
        run: |
          echo "⚙️ Merging configs..."
          
          BATTERY_CONFIG="angelbeats_${PROFILE}.config"
          echo "Using profile: $PROFILE"
          echo "Config file: $BATTERY_CONFIG"
          
          # Merge configs
          cat arch/arm64/configs/gki_defconfig \
              arch/arm64/configs/vendor/shennong_GKI.config \
              arch/arm64/configs/vendor/$BATTERY_CONFIG \
              > .config
          
          echo "✅ Configs merged!"
          echo "Config size: $(wc -l < .config) lines"
          
          # Setup environment
          export PATH="$GITHUB_WORKSPACE/clang/bin:$PATH"
          export ARCH=arm64
          export SUBARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          export CC=clang
          export LLVM=1
          export LLVM_IAS=1
          
          # Resolve config dependencies - với error handling
          echo "Resolving config dependencies..."
          make olddefconfig 2>&1 | tee config.log || {
            echo "⚠️ olddefconfig had warnings, checking if critical..."
            cat config.log
            # Continue even if there are warnings
          }
          
          echo "✅ Config resolved!"
      
      - name: 🏗️ Build kernel
        working-directory: kernel
        run: |
          echo "🏗️ Starting kernel build..."
          
          export PATH="$GITHUB_WORKSPACE/clang/bin:$PATH"
          export ARCH=arm64
          export SUBARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          export CC=clang
          export LLVM=1
          export LLVM_IAS=1
          
          # Show versions
          echo "Compiler version:"
          clang --version | head -1
          echo ""
          echo "Kernel version:"
          make kernelversion
          echo ""
          
          # Build with all cores
          echo "🚀 Building with $(nproc) cores..."
          make -j$(nproc) 2>&1 | tee ../build.log
          
          # Check result
          if [ -f arch/arm64/boot/Image.gz ]; then
            echo ""
            echo "✅ ================================"
            echo "✅  KERNEL BUILD SUCCESSFUL!"
            echo "✅ ================================"
            ls -lh arch/arm64/boot/Image.gz
            echo ""
          else
            echo ""
            echo "❌ ================================"
            echo "❌  BUILD FAILED!"
            echo "❌ ================================"
            echo ""
            echo "Last 200 lines of build log:"
            tail -200 ../build.log
            echo ""
            echo "Errors found:"
            grep -i "error:" ../build.log | tail -20
            exit 1
          fi
      
      - name: 📦 Package with AnyKernel3
        run: |
          echo "📦 Packaging kernel..."
          
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3
          cd AnyKernel3
          
          cp ../kernel/arch/arm64/boot/Image.gz ./
          
          cat > anykernel.sh << 'AKEND'
          properties() { '
          kernel.string=AngelBeats-SukiSU-SUSFS-Xiaomi14Pro
          do.devicecheck=1
          do.modules=0
          do.systemless=1
          do.cleanup=1
          do.cleanuponabort=0
          device.name1=shennong
          device.name2=Xiaomi 14 Pro
          device.name3=2311DRN47C
          supported.versions=14-15
          '; }
          block=/dev/block/by-name/boot;
          is_slot_device=auto;
          ramdisk_compression=auto;
          . tools/ak3-core.sh;
          dump_boot;
          write_boot;
          AKEND
          
          DATE=$(date +%Y%m%d-%H%M)
          ZIPNAME="AngelBeats-SukiSU-Xiaomi14Pro-${PROFILE}-${DATE}.zip"
          
          zip -r9 "../${ZIPNAME}" * -x .git README.md *placeholder
          
          cd ..
          echo "ZIPNAME=${ZIPNAME}" >> $GITHUB_ENV
          
          cat > KERNEL_INFO.txt << 'INFOEND'
          AngelBeats-Style Kernel for Xiaomi 14 Pro
          
          Features:
          - SukiSU Ultra (root)
          - SUSFS v2.0.0 (root hiding)
          - KPM + KProbes support
          - Battery optimization
          - BFQ I/O Scheduler
          
          Device: Xiaomi 14 Pro (shennong)
          Kernel: 6.1.118
          
          Flash via TWRP:
          1. Backup boot.img
          2. Flash ZIP
          3. Reboot
          
          Enjoy better battery life!
          INFOEND
          
          echo "✅ Packaged: ${ZIPNAME}"
          ls -lh "${ZIPNAME}"
      
      - name: 📤 Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Xiaomi-14-Pro-Kernel-${{ env.PROFILE }}
          path: |
            *.zip
            KERNEL_INFO.txt
            build.log
          retention-days: 30
      
      - name: 🎉 Build complete
        run: |
          echo ""
          echo "╔════════════════════════════════════╗"
          echo "║   ✅  BUILD SUCCESSFUL!  ✅       ║"
          echo "╚════════════════════════════════════╝"
          echo ""
          echo "📦 Kernel: ${{ env.ZIPNAME }}"
          echo "⚡ Profile: ${{ env.PROFILE }}"
          echo ""
          echo "📥 Download from Artifacts section"
          echo "🔋 Flash and enjoy!"
          echo ""
