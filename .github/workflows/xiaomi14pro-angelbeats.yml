name: 🔋 Xiaomi 14 Pro - AngelBeats + SukiSU + SUSFS

on:
  workflow_dispatch:
    inputs:
      profile:
        description: 'Battery Profile'
        required: true
        default: 'balanced'
        type: choice
        options:
          - extreme_battery
          - balanced
          - performance
  
  push:
    branches:
      - main
    paths:
      - '.github/workflows/xiaomi14pro-angelbeats.yml'

jobs:
  build:
    name: Build Kernel
    runs-on: ubuntu-22.04
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
      
      - name: Set profile
        run: |
          if [ -z "${{ inputs.profile }}" ]; then
            echo "PROFILE=balanced" >> $GITHUB_ENV
          else
            echo "PROFILE=${{ inputs.profile }}" >> $GITHUB_ENV
          fi
      
      - name: 🧹 Free space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache
          sudo apt-get clean
      
      - name: 📦 Install deps
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential ccache curl flex \
            g++-multilib gcc-multilib git gnupg gperf imagemagick \
            lib32readline-dev lib32z1-dev liblz4-tool libncurses5-dev \
            libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop \
            pngcrush rsync schedtool squashfs-tools xsltproc zip \
            zlib1g-dev python3 python-is-python3 wget cpio kmod \
            libelf-dev libelf1 elfutils
      
      - name: 📂 Clone kernel
        run: |
          git clone --depth=1 \
            https://github.com/MiCode/Xiaomi_Kernel_OpenSource \
            -b shennong-u-oss \
            kernel
          cd kernel
          make kernelversion 2>/dev/null || cat Makefile | head -5
      
      - name: 🔧 Fix Kconfig
        working-directory: kernel
        run: |
          if [ -f "drivers/misc/Kconfig" ]; then
            sed -i '/source "drivers\/misc\/hwid\/Kconfig"/d' drivers/misc/Kconfig
          fi
      
      - name: 🔧 Remove debug_kinfo
        working-directory: kernel
        run: |
          rm -f drivers/android/debug_kinfo.c
          if [ -f "drivers/android/Makefile" ]; then
            sed -i '/debug_kinfo/d' drivers/android/Makefile
          fi
      
      - name: 🔧 Integrate SukiSU
        working-directory: kernel
        run: |
          curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s builtin
          [ -d "KernelSU" ] || exit 1
      
      - name: 📥 Clone SUSFS and integrate
        working-directory: kernel
        run: |
          git clone --depth=1 https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android14-6.1
          
          cd susfs4ksu/kernel_patches
          
          cp -v fs/susfs.c ../../fs/
          mkdir -p ../../include/linux
          cp -v include/linux/susfs_def.h ../../include/linux/
          cp -v include/linux/susfs.h ../../include/linux/
          
          cd ../..
          
          # Add to Makefile
          if ! grep -q "susfs.o" fs/Makefile; then
            echo "obj-\$(CONFIG_KSU_SUSFS) += susfs.o" >> fs/Makefile
          fi
      
      - name: ⚡ Create configs
        working-directory: kernel
        run: |
          mkdir -p arch/arm64/configs/vendor
          
          cat > arch/arm64/configs/vendor/angelbeats_extreme_battery.config << 'END'
          CONFIG_KSU=y
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_SUS_OVERLAYFS=y
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KPM=y
          CONFIG_KPROBES=y
          CONFIG_HAVE_KPROBES=y
          CONFIG_KPROBE_EVENTS=y
          CONFIG_KALLSYMS=y
          CONFIG_KALLSYMS_ALL=y
          CONFIG_CPU_FREQ_GOV_POWERSAVE=y
          CONFIG_CPU_FREQ_GOV_SCHEDUTIL=y
          CONFIG_CPU_FREQ_DEFAULT_GOV_SCHEDUTIL=y
          CONFIG_IOSCHED_BFQ=y
          CONFIG_BFQ_GROUP_IOSCHED=y
          CONFIG_PM_WAKELOCKS_LIMIT=100
          CONFIG_ZRAM=y
          CONFIG_LZ4_COMPRESS=y
          CONFIG_WIREGUARD=y
          CONFIG_TMPFS_XATTR=y
          END
          
          cat > arch/arm64/configs/vendor/angelbeats_balanced.config << 'END'
          CONFIG_KSU=y
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_SUS_OVERLAYFS=y
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KPM=y
          CONFIG_KPROBES=y
          CONFIG_HAVE_KPROBES=y
          CONFIG_KPROBE_EVENTS=y
          CONFIG_KALLSYMS=y
          CONFIG_KALLSYMS_ALL=y
          CONFIG_CPU_FREQ_GOV_SCHEDUTIL=y
          CONFIG_CPU_FREQ_DEFAULT_GOV_SCHEDUTIL=y
          CONFIG_IOSCHED_BFQ=y
          CONFIG_BFQ_GROUP_IOSCHED=y
          CONFIG_PM_WAKELOCKS_LIMIT=200
          CONFIG_ZRAM=y
          CONFIG_LZ4_COMPRESS=y
          CONFIG_WIREGUARD=y
          CONFIG_TMPFS_XATTR=y
          END
          
          cat > arch/arm64/configs/vendor/angelbeats_performance.config << 'END'
          CONFIG_KSU=y
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_SUS_OVERLAYFS=y
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
          CONFIG_KPM=y
          CONFIG_KPROBES=y
          CONFIG_HAVE_KPROBES=y
          CONFIG_KPROBE_EVENTS=y
          CONFIG_KALLSYMS=y
          CONFIG_KALLSYMS_ALL=y
          CONFIG_CPU_FREQ_GOV_SCHEDUTIL=y
          CONFIG_IOSCHED_BFQ=y
          CONFIG_BFQ_GROUP_IOSCHED=y
          CONFIG_PM_WAKELOCKS_LIMIT=500
          CONFIG_ZRAM=y
          CONFIG_LZ4_COMPRESS=y
          CONFIG_WIREGUARD=y
          CONFIG_TMPFS_XATTR=y
          END
      
      - name: 🔨 Clone Clang
        run: |
          git clone --depth=1 https://github.com/kdrag0n/proton-clang clang
      
      - name: ⚙️ Merge configs
        working-directory: kernel
        run: |
          BATTERY_CONFIG="angelbeats_${PROFILE}.config"
          cat arch/arm64/configs/gki_defconfig \
              arch/arm64/configs/vendor/shennong_GKI.config \
              arch/arm64/configs/vendor/$BATTERY_CONFIG \
              > .config
      
      - name: 🛠️ Fix ALL issues
        working-directory: kernel
        run: |
          echo "Applying all fixes..."
          
          scripts/config --disable CONFIG_WERROR
          scripts/config --disable CONFIG_CC_WERROR
          scripts/config --disable CONFIG_SCHED_CLUSTER
          scripts/config --enable CONFIG_OF
          scripts/config --enable CONFIG_OF_EARLY_FLATTREE
          scripts/config --enable CONFIG_OF_FLATTREE
          scripts/config --enable CONFIG_MODULES
          scripts/config --disable CONFIG_DEBUG_KINFO
          scripts/config --disable CONFIG_DEBUG_INFO_BTF
          scripts/config --disable CONFIG_DEBUG_INFO
          scripts/config --enable CONFIG_TMPFS_XATTR
          
          # Keep pkvm and gunyah enabled (they need CONFIG_MODULES)
          scripts/config --enable CONFIG_PKVM
          scripts/config --enable CONFIG_GUNYAH
          
          # Disable module signing to avoid signature issues
          scripts/config --disable CONFIG_MODULE_SIG
          scripts/config --disable CONFIG_MODULE_SIG_ALL
          scripts/config --disable CONFIG_MODULE_SIG_FORCE
          scripts/config --disable CONFIG_SECURITY_LOADPIN
          
          # Force SUSFS to be built-in (=y not =m)
          scripts/config --enable CONFIG_KSU_SUSFS
          scripts/config --enable CONFIG_KSU_SUSFS_SUS_SU
          scripts/config --enable CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT
          scripts/config --enable CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT
          scripts/config --enable CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_HIDE_ZYGISK
          scripts/config --enable CONFIG_KSU_SUSFS_SUS_KLOG
          scripts/config --enable CONFIG_KSU_SUSFS_OPEN_REDIRECT
          scripts/config --enable CONFIG_KSU_SUSFS_SUS_PATH
          scripts/config --enable CONFIG_KSU_SUSFS_SUS_MOUNT
          scripts/config --enable CONFIG_KSU_SUSFS_SUS_KSTAT
          scripts/config --enable CONFIG_KSU_SUSFS_SUS_OVERLAYFS
          scripts/config --enable CONFIG_KSU_SUSFS_TRY_UMOUNT
          scripts/config --enable CONFIG_KSU_SUSFS_SPOOF_UNAME
          scripts/config --enable CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT
          scripts/config --enable CONFIG_KSU_SUSFS_ENABLE_LOG
          
          echo "✅ All fixes applied!"
          
          echo ""
          echo "=== Verifying config ==="
          grep "CONFIG_KSU" .config | grep -v "^#"
      
      - name: 🔧 Patch SUSFS missing functions
        working-directory: kernel
        run: |
          echo "Patching SUSFS missing functions..."
          
          echo "=== Step 1: Analyze current susfs.c ==="
          echo "Lines 835-860:"
          sed -n '835,860p' fs/susfs.c
          
          echo ""
          echo "=== Step 2: Search for all occurrences ==="
          grep -n "susfs_is_avc_log_spoofing_enabled" fs/susfs.c
          
          echo ""
          echo "=== Step 3: Direct patching approach ==="
          
          # Method 1: Make the variable non-static
          if grep -q "^static bool susfs_is_avc_log_spoofing_enabled" fs/susfs.c; then
            echo "Removing static from variable..."
            sed -i 's/^static bool susfs_is_avc_log_spoofing_enabled/bool susfs_is_avc_log_spoofing_enabled/' fs/susfs.c
          fi
          
          # Method 2: Add EXPORT_SYMBOL right after the variable line
          if ! grep -q "EXPORT_SYMBOL.*susfs_is_avc_log_spoofing_enabled" fs/susfs.c; then
            echo "Adding EXPORT_SYMBOL for variable..."
            # Find the line number and add EXPORT after it
            LINE_NUM=$(grep -n "^bool susfs_is_avc_log_spoofing_enabled.*=" fs/susfs.c | head -1 | cut -d: -f1)
            if [ -n "$LINE_NUM" ]; then
              sed -i "${LINE_NUM}a EXPORT_SYMBOL_GPL(susfs_is_avc_log_spoofing_enabled);" fs/susfs.c
            else
              # If pattern not found, try at the end of file
              echo "" >> fs/susfs.c
              echo "// Export for external use" >> fs/susfs.c
              echo "EXPORT_SYMBOL_GPL(susfs_is_avc_log_spoofing_enabled);" >> fs/susfs.c
            fi
          fi
          
          # Method 3: Add susfs_reorder_mnt_id function
          if ! grep -q "void susfs_reorder_mnt_id" fs/susfs.c; then
            echo "Adding susfs_reorder_mnt_id..."
            cat >> fs/susfs.c << 'REORDER'
          
          void susfs_reorder_mnt_id(void)
          {
              return;
          }
          EXPORT_SYMBOL_GPL(susfs_reorder_mnt_id);
          REORDER
          fi
          
          echo ""
          echo "=== Step 4: Update header file ==="
          if [ -f "include/linux/susfs.h" ]; then
            # Remove old declarations if they exist
            sed -i '/extern bool susfs_is_avc_log_spoofing_enabled/d' include/linux/susfs.h
            sed -i '/void susfs_reorder_mnt_id/d' include/linux/susfs.h
            
            # Add new declarations before the last #endif
            sed -i '/^#endif.*SUSFS/i\
          extern bool susfs_is_avc_log_spoofing_enabled;\
          void susfs_reorder_mnt_id(void);' include/linux/susfs.h
          fi
          
          echo ""
          echo "=== Step 5: Verify changes ==="
          echo "Variable in susfs.c:"
          grep -A1 "bool susfs_is_avc_log_spoofing_enabled" fs/susfs.c | head -5
          
          echo ""
          echo "All EXPORT_SYMBOL in susfs.c:"
          grep "EXPORT_SYMBOL" fs/susfs.c
          
          echo ""
          echo "Function susfs_reorder_mnt_id:"
          grep -A3 "void susfs_reorder_mnt_id" fs/susfs.c
          
          echo ""
          echo "Header file declarations:"
          grep -E "susfs_is_avc_log_spoofing_enabled|susfs_reorder_mnt_id" include/linux/susfs.h
          
          echo ""
          echo "=== Step 6: Check if susfs.o will be built ==="
          echo "Makefile entries:"
          grep susfs fs/Makefile
          
          echo ""
          echo "✅ SUSFS patching complete!"
      
      - name: 🔧 Patch PKVM if needed
        working-directory: kernel
        run: |
          if [ -f "arch/arm64/kvm/pkvm.c" ]; then
            echo "Checking PKVM..."
            
            if ! grep -q "pkvm_load_early_modules" arch/arm64/kvm/pkvm.c; then
              echo "Adding pkvm_load_early_modules stub..."
              cat >> arch/arm64/kvm/pkvm.c << 'PKVM_PATCH'
          
          #ifdef CONFIG_MODULES
          int pkvm_load_early_modules(void) {
              return 0;
          }
          #endif
          PKVM_PATCH
              echo "✅ PKVM stub added!"
            else
              echo "✅ PKVM already has the function"
            fi
          fi
      
      - name: 🏗️ Build
        working-directory: kernel
        run: |
          export PATH="$GITHUB_WORKSPACE/clang/bin:$PATH"
          export ARCH=arm64
          export SUBARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          export CC=clang
          export LLVM=1
          export LLVM_IAS=1
          
          clang --version | head -1
          make kernelversion
          
          echo ""
          echo "=== Running olddefconfig ==="
          make olddefconfig 2>&1 | tee ../olddefconfig.log
          
          echo ""
          echo "=== Final .config check ==="
          echo "KSU configs:"
          grep "CONFIG_KSU" .config | head -20
          echo ""
          echo "SUSFS status:"
          grep "CONFIG_KSU_SUSFS" .config || echo "❌ CONFIG_KSU_SUSFS not found!"
          
          echo ""
          echo "=== Checking Makefile ==="
          echo "fs/Makefile SUSFS entries:"
          grep -i susfs fs/Makefile || echo "❌ No SUSFS in fs/Makefile!"
          
          echo ""
          echo "=== Starting kernel build ==="
          make -j$(nproc) \
            KCFLAGS="-Wno-error -Wno-implicit-function-declaration -Wno-sometimes-uninitialized" \
            2>&1 | tee ../build.log
          
          BUILD_EXIT_CODE=${PIPESTATUS[0]}
          
          # Check if Image.gz was created
          if [ -f arch/arm64/boot/Image.gz ]; then
            echo ""
            echo "✅ BUILD SUCCESS!"
            ls -lh arch/arm64/boot/Image.gz
            exit 0
          fi
          
          # If build failed, provide detailed diagnostics
          echo ""
          echo "❌ BUILD FAILED!"
          
          echo ""
          echo "=== Was susfs.o built? ==="
          find . -name "susfs.*" 2>/dev/null | head -10
          
          echo ""
          echo "=== Checking build artifacts ==="
          ls -lh fs/*.o 2>/dev/null | grep susfs || echo "No susfs.o found"
          
          echo ""
          echo "=== Last 150 lines of build log ==="
          tail -150 ../build.log
          
          echo ""
          echo "=== All linker errors ==="
          grep "ld.lld: error" ../build.log || true
          
          exit 1
      
      - name: 📦 Package
        run: |
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3
          cd AnyKernel3
          cp ../kernel/arch/arm64/boot/Image.gz ./
          
          cat > anykernel.sh << 'AK'
          properties() { '
          kernel.string=AngelBeats-SukiSU-Xiaomi14Pro
          do.devicecheck=1
          do.modules=0
          do.systemless=1
          do.cleanup=1
          device.name1=shennong
          device.name2=Xiaomi 14 Pro
          supported.versions=14-15
          '; }
          block=/dev/block/by-name/boot;
          is_slot_device=auto;
          ramdisk_compression=auto;
          . tools/ak3-core.sh;
          dump_boot;
          write_boot;
          AK
          
          DATE=$(date +%Y%m%d-%H%M)
          ZIPNAME="AngelBeats-SukiSU-Xiaomi14Pro-${PROFILE}-${DATE}.zip"
          zip -r9 "../${ZIPNAME}" * -x .git README.md *placeholder
          cd ..
          echo "ZIPNAME=${ZIPNAME}" >> $GITHUB_ENV
      
      - name: 📤 Upload
        uses: actions/upload-artifact@v4
        with:
          name: Kernel-${{ env.PROFILE }}
          path: |
            *.zip
            build.log
      
      - name: 🎉 Done
        run: |
          echo "✅ ${{ env.ZIPNAME }}"
